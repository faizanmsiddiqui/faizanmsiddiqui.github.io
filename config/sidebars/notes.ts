import type { SidebarsConfig } from "@docusaurus/plugin-content-docs";

const notes: SidebarsConfig = {
  seriesSidebar: [
    {
      type: "category",
      label: "Notes",
      link: {
        type: "doc",
        id: "index",
      },
      items: [

      ],
    },
  ],
};

export default notes;
