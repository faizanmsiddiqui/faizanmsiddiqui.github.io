import type { SidebarsConfig } from "@docusaurus/plugin-content-docs";

const articles: SidebarsConfig = {
  seriesSidebar: [
    {
      type: "category",
      label: "Articles",
      link: {
        type: "doc",
        id: "index",
      },
      items: [
      ],
    },
  ],
};

export default articles;
