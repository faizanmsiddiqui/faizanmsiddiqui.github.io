import React, { type ReactNode } from "react";
import { Pushpin } from "@site/src/assets/noto-emoji";

export default function AdmonitionIconInfo(): ReactNode {
  return <Pushpin width={16} height={16} />;
}
